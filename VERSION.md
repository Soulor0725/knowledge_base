# 智慧管理中心 - 版本管理

## 当前版本

**v2.5.5**

---

## 版本历史


---

### v2.5.5 - 2026-07-09

#### 🐛 关键 Bug 修复（6 项）
- **修复死锁**：移除 `_get_cached` 中嵌套的 `with _cache_lock`，消除 `/api/stats` 和 `/api/tags` 路由的线程池卡死风险
- **修复连接泄漏**：`init_db()` 改用 `try-finally` 包裹，异常时保证 SQLite 连接关闭，避免 WAL 锁残留
- **修复竞态条件**：加班记录 `INSERT` 增加 `IntegrityError` 捕获，并发同日提交从 500 错误改为友好的 400 提示
- **修复 XSS**：消费记录和加班记录的 `date`、`remark`、`start_time`、`end_time` 字段改用 `escapeHtml()` 转义后渲染
- **修复信息泄露**：`/api/health` 错误分支移除 `str(e)`，不再向客户端返回数据库内部状态
- **修复索引缺失**：新增表达式索引 `idx_expenses_user_month ON expenses(user_id, substr(date, 6, 2))`，月度统计告别全表扫描

#### 🎨 前端体验优化（5 项）
- 消费记录/加班记录表格列 header 与 body 严格对齐
- 消费记录导出按钮样式与工具栏其他按钮统一
- 销售报表 6 列均分宽度，颜色对比度优化（5斤装紫/10斤装青/总数量浅紫/总金额深绿）
- 销售报表空数据优化：无数据时表头保持、统计归零、不再报错
- 表格数据精度与对齐细节微调

#### 📊 全量审计
- 采用 6 角度并行扫描（安全/性能/质量/业务/前端/配置），从 117 候选验证确认 9 个真实问题
- 新增 7 项检查清单条目（嵌套锁、try-finally、竞态、XSS、异常泄露、索引、空数据）

#### 📝 经验沉淀
- `docs/BUG_FIX_LESSONS.md`：覆盖 v2.1→v2.5.5 全部修复，提炼 15 类 Bug 修复模式 + 测试方法论

### v2.5.4 - 2026-07-08

### 🐛 表单日期默认值修复

#### 新增记录日期字段默认值
- 修复「猕猴桃销售」模块新增订单时 `order_date` 未自动填充今日日期的问题
- 修复「加班记录」模块新增记录时 `date` 未自动填充今日日期的问题
- 「消费记录」模块此前已具备该功能，无需修改
- 实现方式：`openKiwiSalesModal()` 和 `openOvertimeModal()` 中新增 `if (!id)` 判断，对 `date` 输入框设置 `new Date().toISOString().slice(0, 10)`

---

### v2.5.3 - 2026-07-08

### Bug 修复

#### 首次登录后页面滚动锁定（严重）
- 修复 `openLoginModal()` 设置 `document.body.style.overflow = 'hidden'` 后，`closeLoginModal()` 未恢复 body 滚动的问题
- 首次登录关闭登录弹窗后，页面永久无法滚动，右侧滚动条消失，导致"消费统计"等底部模块不可见
- 修复方式：在 `closeLoginModal()` 中添加 `document.body.style.overflow = ''`，与文章详情视图的恢复逻辑保持一致

### 测试验证

#### 全量 API 接口测试
- 涵盖 Auth（注册/登录/个人信息/频率限制/无token异常）、文章 CRUD/搜索/收藏/导航、分类/标签/统计、消费 CRUD/统计/导出、猕猴桃销售 CRUD/报表、加班（工作日/周末）CRUD/统计、上传校验、兼容性测试（CORS/404JSON/405/大数据）、模块切换测试
- 总计 49+ 个接口，正向/异常/逻辑校验均通过

#### Locust 性能测试脚本
- 新增 `locustfile.py`，模拟多用户并发操作所有业务模块

#### Playwright UI 自动化测试脚本
- 新增 `test_ui_e2e.py`，覆盖页面加载、滚动条验证、用户登录、模块导航、CRUD 操作、异常兼容性测试

---

### v2.5.2 - 2026-07-07

### Bug 修复

#### CSV 流式导出 500 错误（致命）
- 修复 `export_expenses` 和 `export_kiwi_sales` 在 generator 迭代时数据库连接已关闭导致 500 的问题
- 新增 `_fetchall_dicts()` 辅助函数：在 cursor 存活期间预取为 dict 列表，generator 仅迭代内存数据
- 修复 CSV 生成器中 `yield` 在 `writerow` 之前导致的首行数据错位 bug

#### 消费记录分页配置统一
- `clamp_pagination()` 新增 `CHOICES = (5, 10, 15)` 校验，非法值自动回落到最接近的合法值
- 消费记录默认每页显示从 10 改为 5，与前端其他模块统一
- 前端分页选择器选项统一为 `[5, 10, 15]`（消费记录、文章、加班等所有列表）
- `safePageSize()` helper 同步更新，防止非法值传入后端

#### 内存泄漏风险
- 导出 LIMIT 从 50000 降为 10000（实测峰值内存从 50-80MB 降至 ~5MB）
- `safe_commit()` 重试次数从 3→2、单次退避从 50ms→30ms，总阻塞从 20s 降至 90ms（< 前端 15s 超时）
- 缓存 `_set_cached()` 新增 `threading.Lock` 防止并发"惊群"重建

#### 启动输出乱码
- 去掉 GBK 乱码的中文 banner，改用 ASCII 启动信息
- `datetime.utcnow()` 全部替换为 `datetime.now(timezone.utc)` 消除 DeprecationWarning

### 性能优化

#### SQLite WAL 模式与 PRAGMA 优化
- 新增 `PRAGMA journal_mode = WAL`（写前日志，提升并发读写性能）
- `PRAGMA busy_timeout = 5000`（自动等待锁释放，避免瞬时冲突报错）
- `PRAGMA wal_autocheckpoint = 500`（更平滑的 checkpoint，减少 I/O 刺突）
- `PRAGMA synchronous = NORMAL`（WAL 模式下安全降低 fsync 频率）
- `check_same_thread=False`（支持多线程共享连接）

#### 新增 4 条复合索引
- `idx_articles_user_updated ON articles(user_id, updated_at DESC)` — 加速文章列表排序
- `idx_kiwi_sales_user_created ON kiwi_sales(user_id, created_at DESC)` — 加速销售单列表
- `idx_expenses_user_date_cat ON expenses(user_id, date)` — 加速消费记录的月份查询
- `idx_expenses_user_yearmonth ON expenses(user_id, substr(date, 1, 7))` — 加速年月统计

#### 日期范围表达式索引失效修复
- 将 `CAST(substr(date, 6, 2) AS INTEGER)` 改为直接字符串比较 `substr(date, 6, 2)`
- 利用 SQLite 字符串序与数字序一致性，命中新建索引

#### 全局 HTTP 错误处理
- 新增 `@app.errorhandler` 处理 400/404/405/413/429/500 状态码
- 统一错误响应为 JSON 格式 `{"error": "..."}`

#### 缓存控制头
- 静态资源 `Cache-Control: public, max-age=86400`（24小时）
- 统计/标签接口 `Cache-Control: private, max-age=60`（1分钟）

### 安全加固

#### 全局速率限制
- 新增 `login_attempts` 内存字典限制同一 IP 登录频率（5分钟/10次）
- IP 记录超 10000 条自动 LRU 淘汰最旧 50%，防止 DoS
- Flask-Compress 启用 Gzip 压缩，降低带宽

#### 单端登录互斥（token_version）
- `users` 表新增 `token_version` 列
- 每次登录成功后 `token_version = token_version + 1`，附带在新 JWT 中
- `login_required` 装饰器每次请求校验 token_version 是否与数据库匹配
- 同一用户在新设备登录后，旧 token 立即 401 ("登录已在其他地方失效，请重新登录")

#### JSON 请求安全解析
- 新增 `safe_get_json()` 统一处理 malformed JSON、空 body、非 JSON 内容类型

### 前端优化

#### DOM 查询缓存
- 新增 `_$(id)` memoize 函数 + `_domCache`，减少重复 DOM 查询
- 高频 `getElementById` 调用替换为 `_$()`

#### API 请求拦截器（apiRequest）
- 统一封装 fetch + 超时 + 重试 + 401 自动跳转登录页
- 自动处理网络断开/恢复事件监听

#### 搜索防抖
- 搜索输入去抖 300ms，避免高频请求

### v2.5.1 - 2026-07-06

### Bug 修复

#### 分类数据泄漏（严重）
- 修复 `get_categories` API 未根据 `user_id` 过滤分类的漏洞，造成用户可看到数据库中所有分类名称【#1】
- 修复 `create_category` 未存储 `user_id` 导致分类共享以及 `UNIQUE` 约束冲突的问题【#2】

#### CSV 导出编码崩溃
- 修复 `export_expenses` 和 `export_kiwi_sales` 使用 GBK 编码时，记录内容含不可编码字符时报错的问题【#3】

#### 数据库连接管理不一致
- 修复 `get_articles` 使用原生 `sqlite3.connect` 而非标准 `get_db()` 的问题，统一连接管理【#4】


#### 文章阅览计数不准
- 修复 `get_article` 返回文章数据时阅览数已提前 +1，导致前端显示的阅览数永远比实际多 1【#6】

#### 记账修复错误消息误导
- 修复 `update_expense` 未输入金额时返回 MISC 400 错误信息的问题，增强空值【#7】

#### 分类删除代码清理
- 移除 `delete_category` 中无意义的死代码（`<int:category_id>` 总是 int 类型）【#8】

#### JWT Bearer Token 解析
- 修复 `login_required` 装饰器中 `token.replace(...)` 替换为 `startswith + 切片` 以避免伪造 token 风险【#9】

#### 安全加固
- 分类接口增加 `user_id` 属性行级约束，避免越权 Access

#### 英文提示全量汉化
- 替换所有用户可见的英文错误提示为中文（`HTTP error!` → `请求失败`、`Error:` → `错误:` 等）【#10】

#### 必填项校验提示精确化
- 登录/注册校验拆分为独立字段提示（`请输入用户名` / `请输入密码`）
- 文章保存新增内容为空校验（`请填写内容`）
- 加班时间拆分为开始/结束时间独立提示
- 分类保存新增名称为空校验（`请输入分类名称`）
- 后端 `create_article` 拆分标题/内容校验提示

#### 加班统计周期修正
- 修正 `get_overtime_monthly_stats` 周期为上月21日至本月20日（含），确保本月20日纳入统计【#5】

### v2.5.0 - 2026-07-05

### ✨ 功能新增

#### 猕猴桃销售 - 运单号物流查询
- 点击运单号直接跳转快递100查询物流信息
- 运单号显示为蓝色可点击链接，带有卡车图标
- 鼠标悬停显示下划线提示

### v2.4.0 - 2026-07-05

### ✨ 功能新增

#### 加班模块 - 手动时长修改
- 加班记录的「时长」字段支持手动修改，默认自动计算
- 编辑弹窗中时长可直接输入，修改后保存即生效
- 列表和统计数据实时同步更新

#### 加班模块 - 月加班统计
- 新增「月加班统计」标签页，按上月21日至本月20日周期统计
- 选择7月 → 统计6月21日至7月20日的总加班时长
- 支持按月筛选，显示统计周期、平时/周末/总加班时长及记录数

### v2.3.0 - 2026-07-01

### 🔒 安全加固

- SECRET_KEY 改为从环境变量读取，无环境变量时随机生成，移除硬编码默认值
- CORS 限定允许源为 localhost:5001，禁止任意域跨域请求
- 移除 `/api/test`、`/api/test2` 调试路由
- 文件上传增加 16MB 大小限制（MAX_CONTENT_LENGTH）
- 注册/登录接口增加 None 空值防护，避免 AttributeError 崩溃
- 新增分类名特殊字符校验，禁止注入 HTML/JS 代码

### 🐛 XSS 修复

- `escapeHtml` 函数升级，支持引号转义（`"` `'`）
- 分类列表：文本内容和 data 属性全部转义，onclick 改用 `dataset.category` 替代内联字符串拼接
- 文章详情页标题转义
- 错误信息展示转义
- 消费分类标签转义

### ✨ 功能优化

#### 记账模块新增分类
- 新增「物业费」「水果」「其他」三个消费分类
- 前后端同步更新分类列表及统计配色
- 新增月度消费趋势柱状图

### 🐛 Bug 修复

#### viewArticle 缺少认证头
- 修复文章详情页 fetch 调用未传 Auth 头，导致未登录用户查看文章静默失败

#### 分类删除后侧栏残留"未分类"
- 修复删除分类后，左侧分类列表仍显示"未分类"条目的问题
- 分类删除时同步将引用该分类的文章设为"未分类"

---

### ✨ 功能新增

#### 记账模块新增分类
- 新增「物业费」「水果」「其他」三个消费分类
- 前后端同步更新分类列表及统计配色

### 🐛 Bug 修复

#### 分类删除后侧栏残留"未分类"
- 修复删除分类后，左侧分类列表仍显示"未分类"条目的问题
- 分类删除时同步将引用该分类的文章设为"未分类"

---

### v2.2.2 - 2026-06-30

### 🎨 UI 优化

- 调亮侧栏模块圆点颜色（加班记录、销售订单、销售报表、消费记录、消费统计），提升深色主题下的视觉辨识度

---

### v2.2.1 - 2026-06-29


### 🐛 修复

- 修复「工作日报」切换视图后每页条数未恢复默认（5条）。


### v2.2.0 - 2026-06-28

### 🐛 重要修复

#### 🔧 数据库连接泄漏修复
- 修复 `close_db` 未注册 `@app.teardown_appcontext` 装饰器的问题
- 修复因数据库连接未自动关闭导致的 `database is locked` 500 错误
- 影响场景：加班/消费模块的删除、批量删除接口在并发或异常路径下偶发失败

### ✨ 功能优化

#### 📚 工作日报文案适配
- 文章模块在「工作日报」分类下，所有用户可见文案中的「文章」自动切换为「日报」：
  - 新增/编辑弹窗标题、标题输入框占位符
  - 列表编辑/删除按钮 tooltip
  - 删除确认、保存成功、加载失败提示
  - 上一篇/下一篇导航到头提示
  - 批量删除确认与成功提示
  - 空列表状态文案
- 顶部统计栏「文章」改为「日报」
- 搜索框占位符「搜索文章...」改为「搜索日报...」
- 修复弹窗标题短暂闪烁问题（编辑时先显示通用占位，异步加载后覆盖）

#### 🎨 UI 全面美化

**布局与间距**
- 压缩侧栏模块间距（padding/margin/圆角收紧），减少滚动
- 用户头像放大（32/36px → 44px），字体同步调大
- 顶部统计栏移至「工作日报」分类内部显示，其他模块自动隐藏

**文章/日报列表**
- 表头升级：13px/600/亮色 + 深色背景，与数据行拉开层级
- 标题列升级：14px/亮色/500 字重，建立「标题 > 次要信息」视觉层级
- 操作按钮放大：padding 6px 10px，图标 14px，hover 蓝（编辑）/红（删除）语义色
- 数据行 hover 改用 CSS：渐变背景 + 左侧蓝色边条 + 内描边 + 平滑过渡

**全系统统一**
- 数据行 hover：加班/消费/销售模块统一改用 CSS class，删除 inline JS 事件
- 操作按钮：全系统统一为图标按钮 + hover 语义色
- 分页按钮：当前页实心蓝白字，其他页 hover 浅色，禁用态半透明
- 空状态：统一为图标 + 虚线卡片 + 引导文案
- 状态标签：猕猴桃订单已发货绿色、未发货蓝色 pill 标签
- 表格圆角：猕猴桃表头风格与文章/加班/消费统一

**配色调整**
- 消费金额、销售支付金额、销售报表总金额：黄色 → 青色 `#2dd4bf`
- 未发货状态标签：黄色 → 蓝色 `#60a5fa`
- 加班时长：绿色 → 蓝色（中性数据语义）
- 侧栏 active 状态：文字变亮 + 字重 600 + dot 光晕加强

**日期选择器**
- 适配深色主题：`color-scheme: dark` + 日历图标蓝色滤镜
- 覆盖所有 5 个日期/月份选择器

**模块标题**
- 加班记录/销售订单/销售报表/消费记录/消费统计 顶部标题统一调大到 18px

**销售订单表单**
- 去掉客户名、电话文本框的默认占位符文字

### 📦 新增文档
- 新增 `CODEBUDDY.md`：CodeBuddy Code 项目指引文档

---

### v2.1.0 - 2026-06-27

### 🐛 重要修复

#### 🔧 导出功能编码问题修复
1. **消费记录导出**
   - 修复Excel打开乱码问题
   - 改用GBK编码（Windows Excel原生支持）
   - 新增UTF-8 BOM标记确保兼容性

2. **猕猴桃销售订单导出**
   - 从前端生成改为后端API处理
   - 新增 `/api/kiwi-sales/export` 后端API
   - 使用GBK编码，完美支持中文显示

#### 🍋 猕猴桃销售模块优化
- 优化导出流程，支持全选和多选导出
- 无选择时导出当前月份数据
- 支持分类筛选后的导出

#### 📋 消费记录模块功能
- 新增消费记录导出功能
- 支持多选、全选、无选择导出当前月
- 完整的消费管理功能

### 🔧 技术改进
- 新增 `codecs` 导入处理编码
- 使用 `BytesIO` + `StringIO` 正确处理CSV流
- 统一Content-Type为 `text/csv; charset=gbk`

---

### v2.0.0 - 2026-04-26

### 🎉 重大更新

#### 🌟 项目更名
- 项目名称从"个人知识库"更名为"智慧管理中心"
- 更准确地反映了系统的综合管理特性

### ✨ 新增功能

#### 🍋 猕猴桃销售管理模块
1. **订单管理**
   - 支持创建、编辑、删除销售订单
   - 客户名称、运单号码管理
   - 规格选择（5斤装/10斤装）

2. **数据字段**
   - 数量管理（整数类型）
   - 支付金额（保留2位小数）
   - 发货状态（已发货/未发货）

3. **实用功能**
   - 导出订单到Excel（CSV格式）
   - 批量删除订单
   - 订单选择导出后自动清除选择并隐藏删除按钮

4. **报表统计**
   - 按客户分组统计
   - 按规格汇总统计
   - 总数量和总金额统计
   - 分页显示，每页10个客户
   - 实时数据同步，订单变更后自动更新报表

#### 📚 知识库模块优化
- 保留所有原有知识库功能
- Markdown编辑器、代码高亮
- 用户认证、分类管理、收藏功能
- 搜索、浏览统计、响应式设计

### 🔧 技术优化

#### 前端优化
1. **布局调整**
   - 优化销售订单列表布局
   - 防止运单号码折行
   - 报表列宽优化，数量列固定80px防止被挤压
   - 金额列自动调整宽度，支持大金额显示

2. **UI/UX改进**
   - 统一表头字体大小为15px
   - 优化报表分页控件
   - 改善按钮点击反馈

#### 后端优化
1. **API增强**
   - 新增销售订单管理API
   - 新增销售报表统计API
   - 支持分页查询

2. **数据库**
   - 自动数据库迁移
   - 新增字段自动添加

### 🐛 问题修复

- 修复订单编辑后数据不同步问题
- 修复删除后删除按钮不自动隐藏问题
- 修复多选功能被意外移除的问题
- 修复备注列不显示内容的问题
- 修复数据库路径错误问题

### 📦 项目配置

- 更新启动信息为"智慧管理中心"
- 更新README.md完整文档
- 项目结构优化整理

---

### v1.0.0 - 2026-04-xx

### ✨ 初始版本

- 个人知识库系统
- Markdown编辑器
- 用户认证
- 分类管理
- 文章收藏
- 搜索功能
- 浏览统计

---

## 版本号规则

| 版本位 | 说明 | 示例 |
|--------|------|------|
| **主版本号** | 重大架构变更 | v2.0.0 |
| **次版本号** | 新增功能模块 | v2.1.0 |
| **修订号** | Bug修复和小改进 | v2.0.1 |

---

## 模块清单

| 序号 | 模块名称 | 状态 | 版本引入 |
|------|---------|------|---------|
| 1 | 核心应用（知识库） | ✅ | v1.0.0 |
| 2 | 猕猴桃销售管理 | ✅ | v2.0.0 |
| 3 | 消费记录管理 | ✅ | v2.1.0 |
| 4 | 加班管理 | ✅ | v2.0.0 |
| 5 | 报表统计 | ✅ | v2.0.0 |
| 6 | 导出功能（编码优化） | ✅ | v2.1.0 |
| 7 | 消费分类扩展 | ✅ | v2.2.3 |
| 8 | 运单号物流查询 | ✅ | v2.5.0 |
